import React from 'react';
import ErrorPage from './ErrorPage';

const Settings = () => {
  return (
    <div>
      <ErrorPage title='Settings' />
    </div>
  );
};

export default Settings;
